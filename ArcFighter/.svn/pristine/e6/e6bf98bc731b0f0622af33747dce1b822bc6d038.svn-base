uses
    osxbgi;
var
    gDriver,gMode: Smallint;
begin
    gDriver := noPalette;       
    gMode := m800x600;          
    initgraph(gDriver,gMode,'');
 
 setcolor(pink);
 line(12, 12, 7,10);
 readkey();
 line(17, 24, 5,10);
 readkey();
 line(30, 5, 12,10);


 
 readkey();                              
    closegraph();
end.